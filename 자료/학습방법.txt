
input 태그를 사용할 때
- value와 onChange로 값을 준다.
- value에는 useState를 통해서 값을 연동해준다.
- 항상 입력시에는 문자열로 입력이 된다.

useEffect
- 특정 값을 계속 감시하다가 그 값이 바뀔 때 이벤트 적용
- 처음 돌 때.
- 뒷정리 함수(addEventListener를 사용할 때)

useState
- 

추가/수정/삭제를 할 때
- 추가: concat
- 수정: map
- 삭제: filter

불변성 => 검색해서 몰라도 익혀두기.

webpack 사용하는데, 왜 사용하는가.
- js가 하나이기 때문에, 처음에 느려.
- 한번에 하기 때문에 더 압축해주어야 한다.
- bundle.js를 잘라서 보관하는 기능
- next.js라는 것, 처음에 빨리 불러주기 위해